{
  "name": "greeter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A tiny greeting library used to exercise the Maestro pipeline.",
  "scripts": {
    "test": "node --test test/*.js"
  }
}
